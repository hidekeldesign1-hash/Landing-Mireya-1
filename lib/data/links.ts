/** External destinations — replace placeholders when live URLs are available. */
export const links = {
  shopify: "#camino-1",
  evaluation: "#camino-2",
  consultation: "#consulta",
  products: "#sintomas",
  systems: "#sistemas",
  startNow: "#caminos",
} as const;
